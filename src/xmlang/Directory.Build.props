<Project>

  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)..'))" />

  <!-- The xmlang family: version minor tracks the spec (xmlang-spec.md); patch is free.
       Release tag xmlang-v*. -->
  <PropertyGroup>
    <Version>0.5.0</Version>
  </PropertyGroup>

</Project>
